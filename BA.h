#ifndef BA_H
#define BA_H
#include "MtoL.h"
void printm(int* matrix, int row_size, int start, int end);
int* makeComplitGraph(int vn);   
int* BAnet(int max_vertex, int complit_graphV, int addn);

#endif //BA_H